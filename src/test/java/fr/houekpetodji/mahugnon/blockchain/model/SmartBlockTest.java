package fr.houekpetodji.mahugnon.blockchain.model;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.util.Assert;


import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class SmartBlockTest {

    @Test
    void testToString() {
      /*  Transaction transaction1 = new Transaction("sender1","receiver1", 1.0);
        Transaction transaction2 = new Transaction("sender2","receiver2",2.0);
        List<Transaction> transactions = Arrays.asList(transaction1,transaction2);
        SmartBlock block = new SmartBlock(1,transactions,"yo");
        Assertions.assertEquals("{index=1, timestamp=Thu Sep 23 01:41:47 CEST 2021, transactions:[\n" +
                "{sender:'sender1', receiver:'receiver1', value:1.0}\n" +
                "{sender:'sender2', receiver:'receiver2', value:2.0}\n" +
                "], previousHash:'yo'}",block.toString());
*/    }
}